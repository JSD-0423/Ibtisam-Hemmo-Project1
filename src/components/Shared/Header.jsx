import React from "react";
import { Navbar, HeaderContent } from "../Shared";

const Header = () => {
  return (
    <header>
      <Navbar/>
      <HeaderContent />
    </header>
  );
};

export default Header;
